# Environment variables for local development
# Copy this file to .env.local and set your values
# Base URL for API requests (leave empty for same-origin via Vite proxy, or set e.g. /api)
VITE_API_BASE_URL=
